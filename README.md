Convenience scripts, either not sure if broadly useful yet or too messy to include as a tool in one of our repos

These scripts rely on packages (e.g. pydicom, pandas) that aren't installed in the system Python. Source `activate_evserver_env.sh` first to use ~/git/EVServer's poetry venv, which already has them:

```bash
source activate_evserver_env.sh
```
